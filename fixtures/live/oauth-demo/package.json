{
  "name": "oauth-demo",
  "version": "0.1.0",
  "description": "offline OAuth demo for Agent Workbench Level 2 acceptance",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
